import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { markRaw } from 'vue';
import { PfFlex, PfFlexItem } from '@core/layouts/Flex/index.js';
import PfSkeleton from '@core/components/Skeleton.vue';

export const Template = (args) => ({
  components: { PfFlex, PfFlexItem },
  setup() {
    return { args };
  },
  template: `
    <pf-flex-item v-bind="args">
      Flex item
    </pf-flex-item>
  `,
});


<Meta title="Layouts/Flex Item" component={PfFlexItem} subcomponents={{PfFlex}} />

# PfFlexItem `pf-flex-item`

## Examples

The dashed lines are not part of the component and are only for visual separation in the documentation. The highlighted item with green lines is the one controlled by the story.

<Canvas withSource="open">
  <Story
    name="Flex Item"
    decorators={[
      () => ({
        components: { PfFlex, PfFlexItem },
        template: `
          <pf-flex class="docs-layout-outline">
            <pf-flex flex="1">
              <story style="border-color: var(--pf-global--palette--green-300)" />
            </pf-flex>
            <pf-flex flex="1">
              <pf-flex-item>Flex item</pf-flex-item>
              <pf-flex-item>Flex item</pf-flex-item>
            </pf-flex>
            <pf-flex flex="1">
              <pf-flex-item>Flex item</pf-flex-item>
              <pf-flex-item>Flex item</pf-flex-item>
              <pf-flex-item>Flex item</pf-flex-item>
            </pf-flex>
          </pf-flex>
        `,
      }),
    ]}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Flex Item" />
