import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import PfBullseye from '@vue-patternfly/core';

export const Template = (args) => ({
  components: { PfBullseye },
  setup() {
    return { args };
  },
  template: `
    <pf-bullseye v-bind="args">
      Bullseye ◎ layout
    </pf-bullseye>
  `,
});


<Meta title="Layouts/Bullseye" component={PfBullseye} />

# PfBullseye `pf-bullseye`

## Examples

The dashed lines are not part of the component and are only for visual separation in the documentation.

<Canvas>
  <Story
    name="Bullseye"
    decorators={[
      () => ({
        template: `
          <story class="docs-layout-outline" />
        `,
      }),
    ]}>
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Bullseye" />
