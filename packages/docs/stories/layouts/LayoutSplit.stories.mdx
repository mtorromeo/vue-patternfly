import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { PfSplit, PfSplitItem } from '@core/layouts/Split/index.js';

export const Template = (args) => ({
  components: { PfSplit, PfSplitItem },
  setup() {
    return { args };
  },
  template: `
    <pf-split v-bind="args">
      <pf-split-item>content</pf-split-item>
      <pf-split-item filled>
        pf-m-fill
      </pf-split-item>
      <pf-split-item>content</pf-split-item>
    </pf-split>
  `,
});


<Meta title="Layouts/Split" component={PfSplit} subcomponents={{PfSplitItem}} />

# PfSplit `pf-split`

## Examples

The dashed lines are not part of the component and are only for visual separation in the documentation.

<Canvas withSource="open">
  <Story
    name="Split"
    args={{gutter: true}}
    decorators={[
      () => ({
        template: `
          <story class="docs-layout-outline" />
        `,
      }),
    ]}>
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Split" />
