import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { PfLevel, PfLevelItem } from '@core/layouts/Level/index.js';

export const Template = (args) => ({
  components: { PfLevel, PfLevelItem },
  setup() {
    return { args };
  },
  template: `
    <pf-level v-bind="args">
      <pf-level-item>Level Item</pf-level-item>
      <pf-level-item>Level Item</pf-level-item>
      <pf-level-item>Level Item</pf-level-item>
    </pf-level>
  `,
});


<Meta title="Layouts/Level" component={PfLevel} subcomponents={{PfLevelItem}} />

# PfLevel `pf-level`

## Examples

The dashed lines are not part of the component and are only for visual separation in the documentation.

<Canvas withSource="open">
  <Story
    name="Level"
    args={{gutter: true}}
    decorators={[
      () => ({
        template: `
          <story class="docs-layout-outline" />
        `,
      }),
    ]}>
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Level" />
