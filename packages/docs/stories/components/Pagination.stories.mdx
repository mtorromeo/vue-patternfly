import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { PfPagination } from '@core/components/Pagination/index.js';
import { ref } from 'vue';

export const Template = (args) => ({
  components: { PfPagination },
  setup() {
    delete args.page;
    const page = ref(1);
    return { args, page };
  },
  template: `
    <div>
      <pf-pagination v-model:page="page" v-bind="args" />
    </div>
  `,
});


<Meta title="Components/Pagination" component={PfPagination} argTypes={{
    page: {
      control: null,
    },
  }} />

# PfPagination `pf-pagination`

## Examples

<Canvas withSource="open">
  <Story name="Pagination" args={{
    count: 333,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Pagination" />
