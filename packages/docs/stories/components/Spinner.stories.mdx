import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import PfSpinner from '@vue-patternfly/core/src/components/Spinner.vue';

export const Template = (args) => ({
  components: { PfSpinner },
  setup() {
    return { args };
  },
  template: `<pf-spinner v-bind="args" />`,
});


<Meta title="Components/Spinner" component={PfSpinner} argTypes={{
    size: {
      control: {
        type: 'select',
      },
      options: ['sm', 'md', 'lg', 'xl'],
    },
  }} />

# PfSpinner `pf-spinner`

## Examples

<Canvas>
  <Story name="Spinner">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Spinner" />
