import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import PfBadge from '@vue-patternfly/core';

export const Template = ({ sample_label, ...args }) => ({
  components: { PfBadge },
  setup() {
    return { args, sample_label };
  },
  template: `<pf-badge v-bind="args">{{ sample_label }}</pf-badge>`,
});


<Meta title="Components/Badge" component={PfBadge} />

# PfBadge `pf-badge`

## Examples

<Canvas>
  <Story name="Badge" args={{
    sample_label: '24',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Badge" />
