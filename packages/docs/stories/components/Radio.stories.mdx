import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import PfRadio from '@core/components/Radio.vue';

export const Template = (args) => ({
  components: { PfRadio },
  setup() {
    return { args };
  },
  template: `<pf-radio v-bind="args" />`,
});


<Meta title="Components/Radio" component={PfRadio} />

# PfRadio `pf-radio`

## Examples

### Basic

<Canvas withSource="open">
  <Story name="Basic" args={{
    id: 'basic-radio',
    label: 'Basic radio',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

### With description

<Canvas withSource="open">
  <Story name="With description" args={{
    id: 'radio-with-description',
    label: 'Radio with description',
    description: 'Single-tenant cloud service hosted and managed by Red Hat that offers high-availability enterprise-grade clusters in a virtual private cloud on AWS or GCP.',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### With description and body

<Canvas withSource="open">
  <Story name="With description and body" args={{
    id: 'radio-with-description-and-body',
    label: 'Radio with description and body',
    description: 'Single-tenant cloud service hosted and managed by Red Hat that offers high-availability enterprise-grade clusters in a virtual private cloud on AWS or GCP.',
    body: 'This is where custom content goes.',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
