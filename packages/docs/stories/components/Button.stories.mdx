import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import PfButton from '@core/components/Button.vue';

export const Template = ({ sample_label, ...args }) => ({
  components: { PfButton },
  setup() {
    return { args, sample_label };
  },
  template: `
    <pf-button v-bind="args">{{ sample_label }}</pf-button>
  `,
});


<Meta title="Components/Button" component={PfButton}  argTypes={{
    variant: {
      control: { type: 'select' },
      options: [
        'primary',
        'secondary',
        'tertiary',
        'danger',
        'warning',
        'link',
        'plain',
        'control',
      ],
    },
    iconPosition: { control: { type: 'select' }, options: ['left', 'right'] },
  }} />

# PfButton `pf-button`

## Examples

### Primary

<Canvas withSource="open">
  <Story name="Primary" args={{
    sample_label: 'Button',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

### Secondary

<Canvas withSource="open">
  <Story name="Secondary" args={{
    sample_label: 'Button',
    variant: 'secondary',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Large

<Canvas withSource="open">
  <Story name="Large" args={{
    sample_label: 'Button',
    large: true,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Small

<Canvas withSource="open">
  <Story name="Small" args={{
    sample_label: 'Button',
    small: true,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
