import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { PfChip } from '@core/components/ChipGroup/index.js';
import PfBadge from '@core/components/Badge.js';

export const Template = ({ sample_label, ...args }) => ({
  components: { PfChip },
  setup() {
    return { args, sample_label };
  },
  template: `
    <pf-chip v-bind="args">{{ sample_label }}</pf-chip>
  `,
});

export const WithBadge = ({ sample_label, ...args }) => ({
  components: { PfChip, PfBadge },
  setup() {
    return { args, sample_label };
  },
  template: `
    <pf-chip v-bind="args">
      {{ sample_label }}
      <pf-badge read>00</pf-badge>
    </pf-chip>
  `,
});


<Meta title="Components/Chip" component={PfChip} />

# PfChip `pf-chip`

## Examples

### Simple

<Canvas withSource="open">
  <Story name="Simple" args={{
    sample_label: 'Chip',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Simple" />

### Long

<Canvas withSource="open">
  <Story name="Long" args={{
    sample_label: 'Really long chip that goes on and on',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### With badge

<Canvas withSource="open">
  <Story name="With badge" args={{
    sample_label: 'Chip',
  }}>
    {WithBadge.bind({})}
  </Story>
</Canvas>
